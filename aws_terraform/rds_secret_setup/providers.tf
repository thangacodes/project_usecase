terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "5.67.0"
    }
  }
}

provider "aws" {
  region = var.region
}

# Note: Please try to use the latest provider version, when you setup Infrastructure in AWS
