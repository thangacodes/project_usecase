{
  "resource": {
    "aws_instance": {
      "web_instance": {
        "ami": "${var.ami_id}",
        "instance_type": "${var.instance_type}",
        "key_name": "${var.key_name}",
        "tags": "${var.tags}"
      }
    }
  }
}
